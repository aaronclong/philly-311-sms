version: "2"
services:
  nginx:
    build: ./nginx
    links:
      - web:web
    ports:
        - "8000:8000"
  web:
    container_name: "web"
    build: ./web
    links:
      - db
    volumes:
    - "./web/src:/usr/src/app"
    ports:
      - 3000
  db:
    container_name: "db"
    build: ./db
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=password
    ports:
      - "5432:5432"
    volumes:
    - "postgres:/var/lib/psql/data"
  

volumes:
  web:
  postgres:
